#include<bits/stdc++.h>
using namespace std;

int T, a, b;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin>>T;
    while(T--){
        cin>>a>>b;
        cout<<a+b<<'\n';
    }
}